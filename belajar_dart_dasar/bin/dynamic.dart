void main() {
  // dynamic is variable declaration that you can change the value and data types to everything you want
  dynamic variable = 100;
  print(variable);

  // the value and data types has changed to boolean
  variable = true;
  print(variable);

  // the value and data types has changed to string
  variable = "Dimas";
  print(variable);
}
